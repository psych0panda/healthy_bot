# HealthCheck Bot

Telegram бот для мониторинга сервисов и получения логов Docker контейнеров.

## Возможности

### 📊 Мониторинг сервисов
- Проверка статуса веб-сервисов
- Мониторинг Docker контейнеров
- Отслеживание процессов
- Сводная статистика по всем сервисам

### 📄 Управление логами
- Просмотр списка всех доступных логов контейнеров
- Интерактивный выбор контейнера для получения лога
- Статус доступности логов (🟢 доступен, 🔴 недоступен)
- Получение последних 1000 строк лога
- Автоматическая отправка больших логов как файлов
- Статистика по размерам и времени обновления логов

## Поддерживаемые контейнеры

Модуль логов работает со следующими контейнерами:
- `infra-compose_api-service_1`
- `infra-compose_neuroboss-service_1`
- `chroma`
- `infra-compose_rag-service_1`
- `infra-compose_agent-service_1`

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd healthcheck
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp env_example.txt .env
# Отредактируйте .env файл
```

5. Убедитесь, что директория логов существует:
```bash
sudo mkdir -p /srv/neuroboss/logs
sudo chown $USER:$USER /srv/neuroboss/logs
```

## Конфигурация

### Переменные окружения (.env файл)

```env
# Токен Telegram бота
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Сервисы для мониторинга (опционально)
SERVICES_TO_MONITOR=web:http://localhost:8080,nginx:docker:nginx,db:docker:postgres
```

### Автоматическое обновление логов

Для автоматического обновления симлинков на логи добавьте в crontab:

```bash
# Обновление каждые 5 минут
*/5 * * * * /path/to/auto_update_simlink.sh
```

## Использование

### Запуск бота

```bash
python healthcheck_bot.py
```

### Команды бота

#### Основные команды
- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/hello` - Поздороваться
- `/time` - Показать текущее время
- `/echo <текст>` - Повторить ваш текст
- `/info` - Информация о боте

#### Команды мониторинга
- `/status` - Проверить статус всех сервисов
- `/services` - Показать список мониторимых сервисов

#### Команды логов
- `/logs` - Получить логи Docker контейнеров

### Примеры использования

1. **Проверка статуса сервисов:**
   ```
   /status
   ```

2. **Получение логов:**
   ```
   /logs
   ```
   Затем выберите нужный контейнер из списка

3. **Повторение текста:**
   ```
   /echo Привет, мир!
   ```

## Структура проекта

```
├── healthcheck_bot.py      # Основной модуль бота
├── service_monitor.py      # Модуль мониторинга сервисов
├── logs_module.py          # Модуль работы с логами
├── auto_update_simlink.sh  # Скрипт обновления симлинков
├── requirements.txt        # Зависимости Python
├── .env                    # Конфигурация (создается из env_example.txt)
├── README.md              # Документация
└── README_logs_module.md  # Документация модуля логов
```

## Архитектура

### HealthCheckBot
Основной класс бота, который:
- Инициализирует все модули
- Настраивает обработчики команд
- Управляет жизненным циклом бота

### ServiceMonitor
Модуль для мониторинга сервисов:
- Проверка HTTP сервисов
- Мониторинг Docker контейнеров
- Отслеживание процессов
- Формирование сводок

### LogsModule
Модуль для работы с логами:
- Интерактивный выбор контейнеров
- Чтение и форматирование логов
- Отправка логов в чат или как файлы
- Статистика по логам

## Безопасность

- Проверка существования файлов перед чтением
- Ограничения на размер отправляемых данных
- Обработка ошибок при чтении файлов
- Временные файлы автоматически удаляются
- Валидация входных данных

## Логирование

Бот использует стандартное логирование Python с уровнем INFO. Логи выводятся в консоль с временными метками.

## Устранение неполадок

### Бот не запускается
1. Проверьте токен в .env файле
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к директории логов

### Логи недоступны (🔴)
1. Проверьте, что скрипт `auto_update_simlink.sh` запущен
2. Убедитесь, что контейнеры работают
3. Проверьте права доступа к директории `/srv/neuroboss/logs`

### Ошибки при мониторинге
1. Проверьте настройки SERVICES_TO_MONITOR
2. Убедитесь, что сервисы доступны
3. Проверьте логи Python приложения

## Разработка

### Добавление новых команд

1. Создайте метод в классе `HealthCheckBot`
2. Добавьте обработчик в `_setup_handlers()`
3. Обновите справку в `help_command()`

### Расширение мониторинга

1. Добавьте новый тип сервиса в `ServiceMonitor`
2. Обновите логику проверки в `check_service()`
3. Добавьте обработку в `check_all_services()`

### Добавление новых контейнеров

1. Обновите список `CONTAINERS` в `logs_module.py`
2. Убедитесь, что скрипт `auto_update_simlink.sh` обновлен
3. Перезапустите бота

## Лицензия

MIT License

